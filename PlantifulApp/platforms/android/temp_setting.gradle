android {
            defaultConfig {
                applicationId = "org.nativescript.PlantifulApp"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }